<Window
    x:Class="Muse_Travel_Manager.Views.BookingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Muse_Travel_Manager.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Muse Travel Manager - Booking"
    Width="1000"
    Height="550"
    MinWidth="800"
    MinHeight="450"
    mc:Ignorable="d">
    <Grid Margin="10">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Оберіть тур:" />
                <TextBlock
                    Margin="3,0"
                    Foreground="Red"
                    Text="*" />
            </StackPanel>
            <ComboBox Name="TourComboBox" Margin="0,5" />

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Оберіть клієнта:" />
                <TextBlock
                    Margin="3,0"
                    Foreground="Red"
                    Text="*" />
            </StackPanel>
            <ComboBox Name="ClientComboBox" Margin="0,5" />

            <TextBlock Text="Дата бронювання:" />
            <TextBox
                Name="BookingDateTextBox"
                Margin="0,5"
                IsReadOnly="True" />

            <Button
                Width="100"
                Margin="5"
                HorizontalAlignment="Center"
                Content="Підтвердити" />
            <Button
                Width="100"
                Margin="5"
                HorizontalAlignment="Center"
                Content="Скасувати" />
        </StackPanel>
    </Grid>
</Window>
